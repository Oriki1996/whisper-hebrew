flask>=3.0.0
python-dotenv>=1.0.0
anthropic>=0.40.0
tqdm>=4.66.0
faster-whisper>=1.0.0

# Vector search & embeddings
sentence-transformers>=3.0.0
numpy>=1.26.0

# Speaker diarization (optional -- requires HF_TOKEN in .env)
# Uncomment to enable: pyannote.audio>=3.3.0
